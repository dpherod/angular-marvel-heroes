{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
      "serve": "json-server --watch db.json --static ./static"
  },
  "dependencies": {
    "json-server": "^0.12.1"
  }
}
